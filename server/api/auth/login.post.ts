// export default defineEventHandler(async event => {
//   const body: { username: string; password: string, isAdmin: boolean } = await readBody(event);
//   const { username, password, isAdmin } = body;
// });